  <%= form_for [@job, @comment] do |f| %>
    <div class='input-group'>
      <span class="input-group-addon" id="basic-addon1">
        <%= f.label "New Comment" %>
      </span>
      <%= f.text_field :content, class: "form-control" %>
    </div>
    
    <%= f.submit class: "btn btn-primary single-button" %>
  <% end %>